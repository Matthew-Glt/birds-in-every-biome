package com.mg3dp.birdsineverybiome.client;

/** Implemented on {@code ParrotRenderState} by ParrotRenderStateMixin so the renderer can see the skin. */
public interface ParrotSkinRenderState {
	int birdsineverybiome$skin();

	void birdsineverybiome$setSkin(int index);
}
